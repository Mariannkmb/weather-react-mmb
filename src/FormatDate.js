import React from "react";

export default function FormatDate(props) {
  //   let day = props.date.getDay();
  //   let month = props.date.getMonth();
  console.log(props.date);

  return <div>"hola"{/* {day} {month} */}</div>;
}
